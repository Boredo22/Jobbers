packages:
  - "apps/*"
  - "packages/*"

# esbuild ships a native binary built via a postinstall script; tsx/drizzle-kit
# need it. pnpm blocks install scripts by default (supply-chain safety); we opt in.
allowBuilds:
  esbuild: true
